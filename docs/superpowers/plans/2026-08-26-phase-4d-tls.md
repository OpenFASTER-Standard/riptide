# Phase 4d (TLS) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add example Kubernetes manifests (Ingress + cert-manager ClusterIssuers) and documentation so a self-hosting operator can terminate TLS in front of Riptide, closing out sub-project 4 (Security & multi-tenancy).

**Architecture:** No Elixir/`lib/` changes — TLS terminates at the ingress, and `config/prod.exs`/`config/runtime.exs` already anticipate this (verified, not assumed — see the spec's Architecture section). This plan only adds two new files under `k8s/`, a one-line documentation update to an existing `k8s/` file, and a new README section. No new automated tests: the spec's Testing section explains why (real ACME issuance needs public DNS + a reachable ingress IP, which this plan does not provision) — verification is `kubectl apply --dry-run=server` against a scratch namespace, run only with explicit human confirmation, plus manual schema review.

**Tech Stack:** Kubernetes (`networking.k8s.io/v1` Ingress), cert-manager (`cert-manager.io/v1` ClusterIssuer, ACME HTTP-01), ingress-nginx annotations.

**Spec:** `docs/superpowers/specs/2026-08-26-phase-4d-tls-design.md`

## Global Constraints

- No changes to any file under `lib/` or `test/` — this phase is manifests and docs only (spec's Architecture section).
- `k8s/cluster-issuer.yaml` is **not** named `.example.yaml` — that suffix is reserved for files containing real secrets that must never be committed (see `k8s/secret.example.yaml`'s own comment). An ACME account email is not a secret, so this file follows the same plain-naming convention as `k8s/ingress.yaml`, `k8s/service.yaml`, and `k8s/statefulset.yaml`.
- The Ingress and ClusterIssuers target **ingress-nginx only** — annotations are `nginx.ingress.kubernetes.io/*`, `ingressClassName: nginx`, and the ClusterIssuer's `http01.ingress.ingressClassName: nginx`. Do not add annotations for other controllers (Traefik, GKE Ingress) — out of scope per the spec.
- Both `letsencrypt-staging` and `letsencrypt-prod` ClusterIssuers must be included — staging exists specifically so an operator can verify HTTP-01 mechanics without risking Let's Encrypt's production rate limits (spec's Manifests section).
- Placeholder values (`riptide.example.com`, `REPLACE_ME@example.com`) must be literal, greppable placeholders an operator edits before applying — not filled in with any real domain or email.
- Any command that touches the live cluster (even a `--dry-run=server` apply) requires explicit human confirmation immediately before running it, per this box's standing operating rule — never chain it automatically after a file-write step.

---

### Task 1: `k8s/ingress.yaml`

**Files:**
- Create: `k8s/ingress.yaml`

**Interfaces:**
- Consumes: the existing `riptide` Service (`k8s/service.yaml`), port 4000 — no changes to that file.
- Produces: an Ingress named `riptide` that Task 2's `letsencrypt-prod` ClusterIssuer is referenced by (via annotation, by name — no direct code dependency).

- [ ] **Step 1: Write the file**

```yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: riptide
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/proxy-buffering: "off"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
spec:
  ingressClassName: nginx
  tls:
    - hosts: ["riptide.example.com"]
      secretName: riptide-tls
  rules:
    - host: riptide.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: riptide
                port:
                  number: 4000
```

- [ ] **Step 2: Validate YAML syntax**

Run: `python3 -c "import yaml, sys; list(yaml.safe_load_all(open('k8s/ingress.yaml')))" && echo OK`
Expected: `OK` (fails loudly on any indentation/syntax error before it ever reaches a cluster).

- [ ] **Step 3: Commit**

```bash
git add k8s/ingress.yaml
git commit -m "Phase 4d: add example Ingress manifest for TLS termination"
```

---

### Task 2: `k8s/cluster-issuer.yaml`

**Files:**
- Create: `k8s/cluster-issuer.yaml`

**Interfaces:**
- Consumes: nothing from other tasks.
- Produces: `letsencrypt-staging` and `letsencrypt-prod` `ClusterIssuer` names, referenced by Task 1's Ingress annotation (`letsencrypt-prod`) and by README documentation in Task 4 (both names, for the staging-first workflow).

- [ ] **Step 1: Write the file**

```yaml
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-staging
spec:
  acme:
    server: https://acme-staging-v02.api.letsencrypt.org/directory
    email: REPLACE_ME@example.com
    privateKeySecretRef:
      name: letsencrypt-staging-account-key
    solvers:
      - http01:
          ingress:
            ingressClassName: nginx
---
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-prod
spec:
  acme:
    server: https://acme-v02.api.letsencrypt.org/directory
    email: REPLACE_ME@example.com
    privateKeySecretRef:
      name: letsencrypt-prod-account-key
    solvers:
      - http01:
          ingress:
            ingressClassName: nginx
```

- [ ] **Step 2: Validate YAML syntax**

Run: `python3 -c "import yaml, sys; list(yaml.safe_load_all(open('k8s/cluster-issuer.yaml')))" && echo OK`
Expected: `OK`

- [ ] **Step 3: Commit**

```bash
git add k8s/cluster-issuer.yaml
git commit -m "Phase 4d: add example cert-manager ClusterIssuers (staging + prod)"
```

---

### Task 3: Document the public-hostname requirement on `PHX_HOST` in `k8s/statefulset.yaml`

**Files:**
- Modify: `k8s/statefulset.yaml` (the existing `PHX_HOST` env var, currently a single `- name: PHX_HOST` / `value: "riptide-headless"` pair with no comment)

**Interfaces:**
- Consumes: nothing new.
- Produces: nothing consumed by other tasks — this is a standalone documentation fix.

- [ ] **Step 1: Read the current `env:` block to confirm the exact surrounding lines**

Run: `grep -n -B1 -A1 'PHX_HOST' k8s/statefulset.yaml`
Expected output (confirm this matches before editing — if it doesn't, stop and re-read the whole file rather than guessing at line numbers):
```
30-            - name: PHX_SERVER
31-              value: "true"
32:            - name: PHX_HOST
33:              value: "riptide-headless"
34-            - name: RELEASE_COOKIE
```

- [ ] **Step 2: Replace the `PHX_HOST` entry with a commented version**

Change:
```yaml
            - name: PHX_HOST
              value: "riptide-headless"
```
to:
```yaml
            # "riptide-headless" (the StatefulSet's own headless Service, k8s/headless-service.yaml)
            # is correct for internal Erlang clustering with no public TLS configured yet. Once
            # you apply k8s/ingress.yaml + k8s/cluster-issuer.yaml, change this to the real public
            # hostname you configured there (e.g. "riptide.example.com") — Socket.check_origin for
            # the /replication WebSocket validates against this value, and force_ssl's redirect
            # target depends on it matching what clients actually connect to.
            - name: PHX_HOST
              value: "riptide-headless"
```

- [ ] **Step 3: Validate YAML syntax**

Run: `python3 -c "import yaml, sys; list(yaml.safe_load_all(open('k8s/statefulset.yaml'))); print('OK')"`
Expected: `OK`

- [ ] **Step 4: Commit**

```bash
git add k8s/statefulset.yaml
git commit -m "Phase 4d: document public-hostname requirement for PHX_HOST"
```

---

### Task 4: "Running via Kubernetes" README section

**Files:**
- Modify: `README.md` (insert a new `## Running via Kubernetes` section between the existing `## Running via Docker` section and the `## Releasing` section)

**Interfaces:**
- Consumes: file names from Tasks 1-3 (`k8s/ingress.yaml`, `k8s/cluster-issuer.yaml`, `k8s/statefulset.yaml`, plus the pre-existing `k8s/service.yaml`, `k8s/headless-service.yaml`, `k8s/secret.example.yaml`) — must reference exactly these names.
- Produces: nothing consumed by other tasks.

- [ ] **Step 1: Confirm the exact insertion point**

Run: `grep -n '^## ' README.md`
Expected: shows `## Running via Docker` and `## Releasing` as adjacent top-level headers (with only the Docker section's body between them) — confirm this before editing so the new section lands in the right place even if the file has drifted since this plan was written.

- [ ] **Step 2: Insert the new section**

Insert this section immediately before the `## Releasing` line found in Step 1:

```markdown
## Running via Kubernetes

Example manifests live in `k8s/` — a 3-replica `StatefulSet` (`k8s/statefulset.yaml`), a
`ClusterIP` Service for client traffic (`k8s/service.yaml`), a headless Service for internal Ra/
Erlang-distribution peer discovery (`k8s/headless-service.yaml`), and a Secret template for the
two required env vars (`k8s/secret.example.yaml`). These assume a Kubernetes cluster and `kubectl`
access already exist — copy `k8s/secret.example.yaml` to `secret.yaml` (git-ignored), fill in real
values per its own comments, then:

```bash
kubectl apply -f k8s/secret.yaml
kubectl apply -f k8s/headless-service.yaml -f k8s/service.yaml -f k8s/statefulset.yaml
```

### TLS

`k8s/ingress.yaml` and `k8s/cluster-issuer.yaml` add TLS termination at the ingress, per this
project's design (see `docs/superpowers/specs/2026-08-26-phase-4d-tls-design.md`) — Riptide itself
never holds a certificate or speaks TLS directly. This requires
[ingress-nginx](https://kubernetes.github.io/ingress-nginx/deploy/) and
[cert-manager](https://cert-manager.io/docs/installation/) already installed on your cluster.

1. Edit `k8s/cluster-issuer.yaml`: replace both `REPLACE_ME@example.com` placeholders with a real
   email address (Let's Encrypt uses this for expiry/problem notifications, not for
   authentication), then `kubectl apply -f k8s/cluster-issuer.yaml`.
2. Edit `k8s/ingress.yaml`: replace both `riptide.example.com` placeholders with your real
   hostname, then `kubectl apply -f k8s/ingress.yaml`.
3. Point that hostname's DNS at your ingress controller's external IP
   (`kubectl get svc -n <ingress-nginx-namespace> ingress-nginx-controller`).
4. Update `k8s/statefulset.yaml`'s `PHX_HOST` value to the same hostname (see the comment above
   that field) and re-apply the StatefulSet.
5. Verify: `kubectl describe certificate riptide-tls` should reach `Ready: True` once cert-manager
   completes the ACME HTTP-01 challenge. While testing, point `k8s/ingress.yaml`'s
   `cert-manager.io/cluster-issuer` annotation at `letsencrypt-staging` instead of
   `letsencrypt-prod` first — Let's Encrypt's production endpoint has strict per-hostname rate
   limits that a first attempt can easily exceed while debugging DNS/ingress setup. Staging certs
   aren't trusted by browsers, so switch the annotation to `letsencrypt-prod` and re-apply once
   staging succeeds.

**Not covered by these manifests:** the subdomain-based tenancy resolver (see
`docs/superpowers/specs/2026-08-26-phase-4a-multi-tenancy-data-model-design.md`) routes tenants by
hostname (`tenant.riptide.example.com`), which needs a *wildcard* certificate — HTTP-01 (used
above) cannot issue wildcards. If you're using that resolver, switch to a DNS-01 solver instead
(provider-specific; see
[cert-manager's ACME DNS-01 provider docs](https://cert-manager.io/docs/configuration/acme/dns01/))
and request `*.riptide.example.com` in `k8s/ingress.yaml`'s `tls.hosts` instead of a single
hostname.
```

- [ ] **Step 3: Confirm the section renders correctly and sits in the right place**

Run: `grep -n '^## ' README.md`
Expected: `## Running via Docker`, `## Running via Kubernetes`, `## Releasing`, in that order, with nothing else reordered.

- [ ] **Step 4: Commit**

```bash
git add README.md
git commit -m "Phase 4d: document Kubernetes deployment, including TLS setup"
```

---

### Task 5: Manifest validation against the live cluster + PROGRESS.md

**Files:**
- Modify: `PROGRESS.md` (sub-project 4's Phase 4d line and Status line, following the exact pattern Phases 4a-4c used)

**Interfaces:**
- Consumes: `k8s/ingress.yaml` and `k8s/cluster-issuer.yaml` from Tasks 1-2.
- Produces: nothing — this is the final task in the phase.

- [ ] **Step 1: Ask for explicit confirmation before touching the live cluster**

This step runs a `--dry-run=server` apply against a scratch namespace on a real, live Kubernetes
cluster. `--dry-run=server` sends the request through the API server's full validation and
admission chain but persists nothing — no object is created. Even so, per this project's standing
rule, state plainly what you're about to run and wait for an explicit go-ahead before running it;
do not chain this step automatically after Tasks 1-4.

- [ ] **Step 2: Create a scratch namespace**

Run: `kubectl create namespace riptide-phase4d-scratch`
Expected: `namespace/riptide-phase4d-scratch created`

- [ ] **Step 3: Dry-run apply both new manifests**

Run: `kubectl apply --dry-run=server -n riptide-phase4d-scratch -f k8s/ingress.yaml -f k8s/cluster-issuer.yaml`
Expected: both resources print with `(server dry run)`, e.g.
`ingress.networking.k8s.io/riptide created (server dry run)` and
`clusterissuer.cert-manager.io/letsencrypt-staging created (server dry run)` /
`clusterissuer.cert-manager.io/letsencrypt-prod created (server dry run)` — this confirms the
manifests are schema-valid against this cluster's actual installed `networking.k8s.io/v1` Ingress
API and cert-manager's `ClusterIssuer` CRD (both present on this cluster as of this plan's
writing). If it instead errors, read the error: a `no matches for kind "ClusterIssuer"` means
cert-manager's CRDs aren't installed wherever this is being run — expected/fine on a cluster
without cert-manager, but re-run against a cluster that has it before trusting the result. This
cluster does not currently run ingress-nginx (no `IngressClass` named `nginx` exists here as of
this plan's writing) — that's fine for this dry-run (core `Ingress` API validation doesn't require
the referenced `IngressClass` to exist), but means this specific cluster cannot be used to
functionally test the manifests end-to-end, matching the spec's decision not to live-test.

- [ ] **Step 4: Clean up the scratch namespace**

Run: `kubectl delete namespace riptide-phase4d-scratch`
Expected: `namespace "riptide-phase4d-scratch" deleted` (a `--dry-run=server` apply creates
nothing, so this mainly guards against having created the namespace itself in Step 2).

- [ ] **Step 5: Update `PROGRESS.md`**

Find the line `- **Phase 4d — TLS.** Not yet designed.` and the line
`**Status**: Phases 4a-4c shipped 2026-08-26. Phase 4d not yet designed.` in the sub-project 4
section. Replace the first with:

```markdown
- **Phase 4d — TLS.** **Shipped 2026-08-26** — see
  `docs/superpowers/specs/2026-08-26-phase-4d-tls-design.md`. TLS terminates at the Kubernetes
  ingress, not in-app — no Elixir changes, confirmed by reading (not assuming) that
  `config/prod.exs`'s `force_ssl` and `config/runtime.exs`'s `url: [scheme: "https"]` already
  anticipate this. New example manifests: `k8s/ingress.yaml` (ingress-nginx, with
  `proxy-buffering: "off"` and a 3600s `proxy-read-timeout` for Riptide's long-lived SSE/WebSocket
  connections) and `k8s/cluster-issuer.yaml` (Let's Encrypt via ACME HTTP-01, staging + prod).
  Wildcard/DNS-01 certs for the subdomain tenancy resolver are deliberately out of scope — see the
  design spec's Out of scope section. Verified via `kubectl apply --dry-run=server` against a
  scratch namespace on a live cluster (schema-valid), not a live-issued certificate — real ACME
  issuance needs public DNS + a reachable ingress IP, which this phase does not provision.
```

and the second with:

```markdown
**Status**: Phases 4a-4d shipped 2026-08-26. Sub-project 4 (Security & multi-tenancy) complete.
```

- [ ] **Step 6: Commit**

```bash
git add PROGRESS.md
git commit -m "Phase 4d: mark TLS shipped, sub-project 4 complete"
```
